RewriteEngine On
RewriteBase /shop/

# Route /shop/{slug} -> /shop/index.php?slug={slug}
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([a-z0-9_-]+)/?$ index.php?slug=$1 [QSA,L]
